diff --git a/mfcs-omega/src/lib/oracleKernelCore.ts b/mfcs-omega/src/lib/oracleKernelCore.ts
--- a/mfcs-omega/src/lib/oracleKernelCore.ts
+++ b/mfcs-omega/src/lib/oracleKernelCore.ts
@@ -1 +1 @@
-export type OracleMode = 'BUILD_COMPRESS' | 'FUSION' | 'ANALYZE' | 'IDLE'; export interface OracleState { mode: OracleMode; } export interface FacetClassification { facet: string; label: string; M_min: number; } export interface EnvelopeInputs { W: number; inside: boolean; margin: number; gap: number; kink: number | null; weights: { C_r: number }; classification: FacetClassification; state: OracleState; } export interface EnvelopeReport { W: number; M_min: number; inside: boolean; margin: number; activeFacet: string; facetLabel: string; supportGap: number; kinkProximity: number; dimension: 2 | 3; degenerate?: boolean; // Law φ-A binding inPhiAttractor: boolean; attractorId?: 'G_phi'; lawCompliance?: { lawId: 'phi-A'; nearRecursion: boolean; irreversible: boolean; inAttractor: boolean; }; } const PHI_FACETS = ['Facet-A', 'Facet-C']; export class OracleKernelCore { constructor( private readonly dimension: 2 | 3, private readonly degenerate?: boolean, ) {} evaluate(input: EnvelopeInputs): EnvelopeReport { const { W, classification, inside, margin, gap, kink, weights, state } = input; const inPhiAttractor = inside && PHI_FACETS.includes(classification.facet); const lawCompliance = { lawId: 'phi-A' as const, nearRecursion: false, irreversible: state.mode === 'BUILD_COMPRESS' || state.mode === 'FUSION', inAttractor: inPhiAttractor, }; return { W, M_min: classification.M_min, inside, margin, activeFacet: classification.facet, facetLabel: classification.label, supportGap: gap, kinkProximity: kink !== null ? Math.abs(weights.C_r - kink) : Infinity, dimension: this.dimension, degenerate: this.degenerate, inPhiAttractor, attractorId: inPhiAttractor ? 'G_phi' : undefined, lawCompliance, }; } }+export type OracleMode = 'BUILD_COMPRESS' | 'FUSION' | 'ANALYZE' | 'IDLE'; export interface OracleState { mode: OracleMode; } export interface FacetClassification { facet: string; label: string; M_min: number; } export interface EnvelopeInputs { W: number; inside: boolean; margin: number; gap: number; kink: number | null; weights: { C_r: number }; classification: FacetClassification; state: OracleState; } export interface EnvelopeReport { W: number; M_min: number; inside: boolean; margin: number; activeFacet: string; facetLabel: string; supportGap: number; kinkProximity: number; dimension: 2 | 3; degenerate?: boolean; // Law φ-A binding inPhiAttractor: boolean; attractorId?: 'G_phi'; lawCompliance?: { lawId: 'phi-A'; nearRecursion: boolean; irreversible: boolean; inAttractor: boolean; }; } const PHI_FACETS = ['Facet-A', 'Facet-C'] as const; export class OracleKernelCore { constructor( private readonly dimension: 2 | 3, private readonly degenerate?: boolean, ) {} evaluate(input: EnvelopeInputs): EnvelopeReport { const { W, classification, inside, margin, gap, kink, weights, state } = input; const inPhiAttractor = inside && PHI_FACETS.includes(classification.facet as (typeof PHI_FACETS)[number]); const lawCompliance = { lawId: 'phi-A' as const, // TODO(phi-A): near-recursion is intentionally deferred until the formal Distance(f^n(x), x) <= ε check is modeled or supplied as an input. nearRecursion: false, irreversible: state.mode === 'BUILD_COMPRESS' || state.mode === 'FUSION', inAttractor: inPhiAttractor, }; return { W, M_min: classification.M_min, inside, margin, activeFacet: classification.facet, facetLabel: classification.label, supportGap: gap, kinkProximity: kink !== null ? Math.abs(weights.C_r - kink) : Infinity, dimension: this.dimension, degenerate: this.degenerate, inPhiAttractor, attractorId: inPhiAttractor ? 'G_phi' : undefined, lawCompliance, }; } }diff --git a/mfcs-omega/src/lib/oracleKernelCore.test.ts b/mfcs-omega/src/lib/oracleKernelCore.test.ts
--- a/mfcs-omega/src/lib/oracleKernelCore.test.ts
+++ b/mfcs-omega/src/lib/oracleKernelCore.test.ts
@@ -1 +1 @@
-import assert from 'node:assert/strict'; import { OracleKernelCore } from './oracleKernelCore'; const core = new OracleKernelCore(2); const facetA = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-A', label: 'A', M_min: 2 }, state: { mode: 'BUILD_COMPRESS' }, }); assert.equal(facetA.inPhiAttractor, true); assert.equal(facetA.attractorId, 'G_phi'); const facetB = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-B', label: 'B', M_min: 2 }, state: { mode: 'ANALYZE' }, }); assert.equal(facetB.inPhiAttractor, false); assert.equal(facetB.attractorId, undefined); console.log('oracleKernelCore attractor tests passed');+import assert from 'node:assert/strict'; import { OracleKernelCore } from './oracleKernelCore'; const core = new OracleKernelCore(2); const facetA = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-A', label: 'A', M_min: 2 }, state: { mode: 'BUILD_COMPRESS' }, }); assert.equal(facetA.inPhiAttractor, true); assert.equal(facetA.attractorId, 'G_phi'); assert.equal(facetA.lawCompliance?.lawId, 'phi-A'); assert.equal(facetA.lawCompliance?.irreversible, true); assert.equal(facetA.lawCompliance?.nearRecursion, false); assert.equal(facetA.lawCompliance?.inAttractor, true); const facetB = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-B', label: 'B', M_min: 2 }, state: { mode: 'ANALYZE' }, }); assert.equal(facetB.inPhiAttractor, false); assert.equal(facetB.attractorId, undefined); assert.equal(facetB.lawCompliance?.lawId, 'phi-A'); assert.equal(facetB.lawCompliance?.irreversible, false); assert.equal(facetB.lawCompliance?.nearRecursion, false); assert.equal(facetB.lawCompliance?.inAttractor, false); const facetC = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-C', label: 'C', M_min: 2 }, state: { mode: 'BUILD_COMPRESS' }, }); assert.equal(facetC.inPhiAttractor, true); assert.equal(facetC.attractorId, 'G_phi'); assert.equal(facetC.lawCompliance?.lawId, 'phi-A'); assert.equal(facetC.lawCompliance?.irreversible, true); assert.equal(facetC.lawCompliance?.nearRecursion, false); assert.equal(facetC.lawCompliance?.inAttractor, true); const phiOutside = core.evaluate({ W: 1, inside: false, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-C', label: 'C', M_min: 2 }, state: { mode: 'BUILD_COMPRESS' }, }); assert.equal(phiOutside.inPhiAttractor, false); assert.equal(phiOutside.attractorId, undefined); assert.equal(phiOutside.lawCompliance?.lawId, 'phi-A'); assert.equal(phiOutside.lawCompliance?.irreversible, true); assert.equal(phiOutside.lawCompliance?.nearRecursion, false); assert.equal(phiOutside.lawCompliance?.inAttractor, false); const fusion = core.evaluate({ W: 1, inside: true, margin: 0.1, gap: 0, kink: null, weights: { C_r: 1 }, classification: { facet: 'Facet-A', label: 'A', M_min: 2 }, state: { mode: 'FUSION' }, }); assert.equal(fusion.inPhiAttractor, true); assert.equal(fusion.attractorId, 'G_phi'); assert.equal(fusion.lawCompliance?.lawId, 'phi-A'); assert.equal(fusion.lawCompliance?.irreversible, true); assert.equal(fusion.lawCompliance?.nearRecursion, false); assert.equal(fusion.lawCompliance?.inAttractor, true); console.log('oracleKernelCore attractor tests passed');diff --git a/mfcs-omega/src/pages/OracleWorkbenchPage.tsx b/mfcs-omega/src/pages/OracleWorkbenchPage.tsx
--- a/mfcs-omega/src/pages/OracleWorkbenchPage.tsx
+++ b/mfcs-omega/src/pages/OracleWorkbenchPage.tsx
@@ -1 +1 @@
-import React from 'react'; type Evaluation = { inPhiAttractor?: boolean; attractorId?: string; lawCompliance?: { nearRecursion: boolean; irreversible: boolean; }; }; type Props = { evaluation: Evaluation; }; function Badge({ children, title }: { children: React.ReactNode; title?: string }) { return {children}; } function Tooltip({ children, content }: { children: React.ReactNode; content: string }) { return {children}; } function Icon({ name }: { name: string }) { return ◈; } export default function OracleWorkbenchPage({ evaluation }: Props) { return (  {evaluation.inPhiAttractor && (  G_φ Active · {evaluation.attractorId}  )} {evaluation.lawCompliance && (    )}  ); }+import React from 'react'; import type { EnvelopeReport } from '../lib/oracleKernelCore'; type Evaluation = Pick<EnvelopeReport, 'inPhiAttractor' | 'attractorId' | 'lawCompliance'>; type Props = { evaluation: Evaluation; }; function Badge({ children, title }: { children: React.ReactNode; title?: string }) { return {children}; } function Tooltip({ children, content }: { children: React.ReactNode; content: string }) { return {children}; } function Icon({ name }: { name: string }) { return ◈; } export default function OracleWorkbenchPage({ evaluation }: Props) { const attractorLabel = evaluation.attractorId ?? 'unknown'; return (  {evaluation.inPhiAttractor && (  G_φ Active · {attractorLabel}  )} {evaluation.lawCompliance && (    )}  ); } }